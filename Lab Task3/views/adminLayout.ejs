<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Sidebar Layout</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">


    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #ff8c00;
            --background-light: #ecf0f1;
            --text-dark: #2c3e50;
            --text-muted: #7f8c8d;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
            background-color: var(--background-light) !important;
            margin: 0 !important;
            padding: 0 !important;
            display: flex !important;
            flex-direction: column !important;
        }


        .admin-sidebar {
            width: 280px;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #34495e;
            box-shadow: 4px 0 10px rgba(0, 0, 0, 0.1);
            overflow-y: auto;
            z-index: 1;
        }

        .sidebar-profile {
            padding: 25px;
            text-align: center;
            border-bottom: 1px solid #2c3e50;
            background-color: #2c3e50;
            color: white;
        }

        .profile-image {
            width: 100px;
            height: 100px;
            border-radius: 15px;
            object-fit: cover;
            margin-bottom: 10px;
            border: 3px solid white;
        }

        .sidebar-menu {
            padding: 15px;
        }

        .sidebar-nav-item {
            text-decoration: none;
            color: #ecf0f1;
            padding: 12px 15px;
            display: flex;
            align-items: center;
            border-radius: 5px;
            transition: background-color 0.3s ease, color 0.3s ease;
            font-weight: 500;
            margin-bottom: 5px;
        }

        .sidebar-nav-item:hover,
        .sidebar-nav-item.active {
            background-color: var(--secondary-color);
            color: #2c3e50;
        }

        .accordion-button {
            font-weight: 500;
            background: none;
            border: none;
            padding: 12px 15px;
            color: #ecf0f1;
            transition: color 0.3s ease, background-color 0.3s ease;
        }

        .accordion-button:hover,
        .accordion-button:not(.collapsed) {
            background-color: var(--secondary-color);
            color: #2c3e50;
        }

        .accordion-body {
            padding-left: 20px;
        }

        .sidebar-dropdown-item {
            padding: 8px 40px;
            color: var(--text-muted);
            text-decoration: none;
            display: block;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .sidebar-dropdown-item:hover {
            background-color: var(--secondary-color);
            color: white;
        }

        .sidebar-menu p {
            color: var(--secondary-color);
            font-weight: bold;
        }

        .main-content {
            margin-left: 280px;
            padding: 90px 30px 30px;
            flex-grow: 1;
            overflow-y: auto;
            background-color: var(--background-light);
        }

        .content-section-header {
            font-size: 24px;
            color: var(--primary-color);
            margin-bottom: 20px;
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 10px;
        }

        .header-strip {
            background-color: var(--primary-color);
            color: white;
            text-align: center;
            padding: 15px 0;
            font-size: 24px;
            font-weight: bold;
            position: fixed;
            top: 0;
            left: 280px;
            width: calc(100% - 280px);
            z-index: 2;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        a{
            text-decoration:none ;
            color: whitesmoke;
        }

        .header-strip .logo {
            position: absolute;
            left: 20px;
            width: 40px;
            height: 40px;
            background-color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--secondary-color);
            font-size: 24px;
        }

    </style>
</head>
<body>

<!-- Header Strip (Fixed at the top, after the sidebar) -->
<div class="header-strip">
    <!-- User Logo (White color circle) -->
    <div class="logo">
        <i class="fas fa-user"></i> <!-- User icon -->
    </div>

    GOURMET FOODS ADMIN PANEL
</div>

<div class="admin-sidebar">
    <!-- Profile Section -->
    <div class="sidebar-profile">
        <h5 class="fw-bold">Areej Fatima</h5>
        <p class="text-light">Admin</p>
    </div>

    <!-- Sidebar Menu -->
    <div class="sidebar-menu">
        <!-- Main Section -->
        <p class="text-uppercase small">Main</p>
        <a href="/admin/dashboard" class="sidebar-nav-item active">
            <i class="fas fa-home me-3"></i> Dashboard
        </a>

        <!-- Product Management -->
        <p class="text-uppercase small mt-4">Product Management</p>

        <div class="accordion" id="productAccordion">
            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#productsDropdown" aria-expanded="false">
                        <i class="fas fa-box me-3"></i> Products
                    </button>
                </h2>
                <div id="productsDropdown" class="accordion-collapse collapse" data-bs-parent="#productAccordion">
                    <div class="accordion-body">
                        <a href="/admin/products/create" class="sidebar-dropdown-item">Add Product</a>
                        <a href="/admin/products" class="sidebar-dropdown-item">View Products</a>
                    </div>
                </div>
            </div>

            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#categoriesDropdown" aria-expanded="false">
                        <i class="fas fa-tags me-3"></i> Categories
                    </button>
                </h2>
                <div id="categoriesDropdown" class="accordion-collapse collapse" data-bs-parent="#productAccordion">
                    <div class="accordion-body">
                        <a href="/admin/products/createCategory" class="sidebar-dropdown-item">Add Category</a>
                        <a href="/admin/viewcatagories" class="sidebar-dropdown-item">View Categories</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="p-3 border-top">
        <button   class="btn btn-danger w-100 rounded-pill">
            <i class="fas fa-sign-out-alt me-2"></i> <a href="/">Logout</a>
        </button>
    </div>
</div>

<div class="main-content">
    <%-body  %>
</div>

<!-- Optional JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

